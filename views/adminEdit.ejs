<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit: <%= ngoParticular.name %></title>
    <link rel="stylesheet" href="../css/style_adminDashboard.css">
</head>

<body>
    <div class="contain" style="display: flex;">
        <section class="main">
            <div class="main-top">
                <h1 class="head">Edit NGO</h1>
            </div>
            <hr>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/adminDashboard">Dashboard</a></li>
                    <li class="breadcrumb-item active" aria-current="page">
                        <%= ngoParticular.name %>
                    </li>
                </ol>
            </nav>
            <form action="/adminedit/<%= ngoParticular._id %>?_method=PATCH" method="post">
                <div class="row form-group mb-4">
                    <div class="col">
                        <label for="Name"> Name</label>
                        <input type="text" class="form-control" id="Name" name="name" value="<%= ngoParticular.name %>"
                            placeholder="Name" required>
                    </div>

                    <div class="col">
                        <label for="Email">Email</label>
                        <input type="email" class="form-control" id="Email" name="email"
                            value="<%= ngoParticular.email %>" placeholder="Email" required>
                    </div>
                </div>

                <div class="row form-group mb-4">
                    <div class="col">
                        <label for="tel">NGO_registration_number</label>
                        <input type="text" class="form-control" id="tel" name="NGO_registration_number"
                            value="<%= ngoParticular.NGO_registration_number %>" placeholder="NGO_registration_number"
                            required>
                    </div>

                    <div class="form-group mb-4">
                        <label for="NGO_range">NGO_range</label>
                        <input type="number" class="form-control" id="NGO_range" name="NGO_range"
                            value="<%= ngoParticular.NGO_range %>" placeholder="NGO_range" required>
                    </div>

                    <div class="form-group mb-4">
                        <label for="NGO_date_of_joining">NGO date of joining</label>
                        <input type="text" class="form-control" id="NGO_date_of_joining" name="NGO_date_of_joining"
                            value="<%= ngoParticular.NGO_date_of_joining %>" placeholder=" NGO date of joining" disabled>
                    </div>

                    <div class="form-group mb-4">
                        <label for="NGO_sectors"> NGO sectors</label>
                        <input type="text" class="form-control" id="NGO_sectors" name="NGO_sectors"
                            value="<%= ngoParticular.NGO_sectors %>" placeholder=" NGO_sectors" required>
                    </div>

                    <div class="form-group mb-4">
                        <label for="NGO_address"> NGO Address</label>
                        <input type="text" class="form-control" id="NGO_address" name="NGO_address"
                            value="<%= ngoParticular.NGO_address.humanReadableAddress %>" placeholder="NGO address" required>
                    </div>

                    <div class="form-group mb-4">
                        <button type="submit" class="btn btn-primary">Update Ngo</button>
                        <button type="button" class="btn btn-danger" data-bs-toggle="modal" id="deleteButton"
                            data-bs-target="#deleteModal">Delete Ngo</button>
                        <a href="/adminDashboard" style="text-decoration: none;" class="btn btn-primary">Cancle</a>
                    </div>

            </form>
            <!-- id shou;ld be same for button that is deleteModel -->
            <div class="modal fade" tabindex="-1" role="dialog" id="deleteModal">
                <div class="modal-dialog" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <div class="modal-title">You are about to remove a NGO record.</div>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                        </div>
                        <div class="modal-body">
                            <p>
                                This will remove the NGO record of 
                                <b class="fw-bold"><%= ngoParticular.name %> and Registration No <%= ngoParticular.NGO_registration_number %></b><br />
                                Are you sure?
                            </p>
                        </div>

                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                            <form action="/adminedit/<%= ngoParticular._id %>?_method=DELETE" method="POST"
                                class="position-relative">
                                <button type="submit" class="btn btn-primary">Yes, Remove Customer</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>

</body>
</html>